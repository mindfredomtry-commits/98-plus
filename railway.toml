# API service — Railway Root Directory must be the repository root (leave blank / ".").
# Installs workspaces from package-lock.json, builds shared + api, starts compiled entrypoint.
[build]
buildCommand = "npm ci --include=dev && npm run build:api"

[deploy]
startCommand = "npm run start:api"
