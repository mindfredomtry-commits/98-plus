# @98plus/api — Railway service Root Directory: repo root ("." / blank).
# Default Config as Code path for this service: /railway.toml
# Installs devDependencies (prisma, typescript) during build, then builds shared + api.
[build]
buildCommand = "npm ci --include=dev && npm run build:api"

[deploy]
startCommand = "npm run start:api"
