# @98plus/web — Railway service at repo root ("." / blank).
# Builds via Dockerfile; Next standalone CMD is defined in apps/web/Dockerfile.
# Do NOT set build:api / start:api here — those are for @98plus/api only.
[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/web/Dockerfile"
