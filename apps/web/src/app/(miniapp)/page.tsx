/**
 * Next.js route module for the miniapp segment.
 * Application UI is owned exclusively by AppServicesProvider → ApplicationSurface.
 * This page is intentionally not mounted (MiniAppLayout does not render children).
 */
export default function Page() {
  return null;
}
