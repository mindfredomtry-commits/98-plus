/**
 * Stage 7 Phase 1 — shell visibility policy removed.
 * File retained as stub so accidental imports fail loudly.
 */
export function resolveShellVisibility(): never {
  throw new Error(
    'shell-visibility removed in Stage 7 Phase 1 — no Lobby/chrome policy in Runtime',
  );
}
