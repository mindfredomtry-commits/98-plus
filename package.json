{
  "name": "98plus",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "dev:web": "npm run dev -w @98plus/web",
    "dev:api": "npm run dev -w @98plus/api",
    "build": "npm run build --prefix packages/shared && npm run build --prefix apps/api && npm run build --prefix apps/web",
    "build:api": "npm run build --prefix packages/shared && npm run db:generate --prefix apps/api && npm run build --prefix apps/api && npm run build:verify --prefix apps/api",
    "start:api": "node apps/api/dist/index.js",
    "db:generate": "npm run db:generate --prefix apps/api",
    "db:migrate": "npm run db:migrate --prefix apps/api",
    "db:push": "npm run db:push --prefix apps/api",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
